# utils.js 工具库使用指南

## 📦 简介

`utils.js` 是一个为A.O.史密斯提成计算工具开发的公共函数库，提供了消息提示、进度条、数据校验、Excel处理等常用功能。

## 🚀 快速开始

### 引入工具库

在HTML文件中引入：

```html
<!-- 先引入XLSX库（如果需要Excel功能） -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js"></script>

<!-- 引入工具库 -->
<script src="utils.js"></script>
```

### 基本使用

```javascript
// 工具库会自动注册到 window.AOSmithUtils
const { showToast, Storage, Validator } = window.AOSmithUtils;

// 显示成功消息
showToast('操作成功！', 'success');

// 保存数据
Storage.set('myData', { name: '张三', age: 25 });

// 读取数据
const data = Storage.get('myData');
```

---

## 📚 API 文档

### 1. 消息提示 (showToast)

显示漂亮的Toast消息提示。

#### 基本用法

```javascript
// 成功消息
showToast('数据保存成功', 'success');

// 错误消息
showToast('文件上传失败', 'error');

// 警告消息
showToast('数据可能不完整', 'warning');

// 信息消息
showToast('正在处理，请稍候...', 'info');
```

#### 高级用法

```javascript
showToast('操作成功', 'success', {
    duration: 5000,      // 显示时长（毫秒），0为不自动关闭
    closable: true,      // 是否显示关闭按钮
    position: 'top-right' // 位置（暂未实现）
});
```

#### 效果预览

![Toast消息](https://via.placeholder.com/400x100?text=Success+Toast)

---

### 2. 进度条 (ProgressBar)

显示操作进度的遮罩层和进度条。

#### 基本用法

```javascript
// 显示进度条
window.progressBar.show('正在处理数据...');

// 更新进度
window.progressBar.update(50, '已完成50%');

// 隐藏进度条
window.progressBar.hide();
```

#### 实际应用示例

```javascript
async function processLargeData(data) {
    const total = data.length;
    const batchSize = 500;
    
    // 显示进度
    window.progressBar.show('正在处理数据...');
    
    for (let i = 0; i < total; i += batchSize) {
        const batch = data.slice(i, i + batchSize);
        
        // 处理批次数据
        await processBatch(batch);
        
        // 更新进度
        const percent = ((i + batchSize) / total) * 100;
        window.progressBar.update(percent, `已处理 ${i + batchSize}/${total} 条`);
        
        // 释放UI线程
        await new Promise(resolve => setTimeout(resolve, 0));
    }
    
    window.progressBar.hide();
    showToast('处理完成', 'success');
}
```

---

### 3. 本地存储 (Storage)

简化的localStorage操作接口。

#### 保存数据

```javascript
// 基本保存
Storage.set('username', '张三');

// 保存对象
Storage.set('userInfo', {
    name: '张三',
    role: '促销员',
    store: '中冠专卖店'
});

// 设置过期时间（7天后过期）
Storage.set('tempData', { value: 123 }, {
    expires: Date.now() + 7 * 24 * 60 * 60 * 1000
});

// 设置版本号
Storage.set('config', configData, {
    version: '2.0'
});
```

#### 读取数据

```javascript
// 读取数据
const username = Storage.get('username');

// 读取数据（带默认值）
const config = Storage.get('config', getDefaultConfig());

// 读取对象
const userInfo = Storage.get('userInfo');
console.log(userInfo.name); // '张三'
```

#### 其他操作

```javascript
// 删除数据
Storage.remove('tempData');

// 清空所有数据
Storage.clear();

// 获取所有键
const keys = Storage.keys();
console.log(keys); // ['username', 'userInfo', 'config']
```

---

### 4. 数据校验 (Validator)

提供常用的数据校验方法。

#### 基本校验

```javascript
const { Validator } = window.AOSmithUtils;

// 检查是否为空
Validator.isEmpty('');        // true
Validator.isEmpty(null);      // true
Validator.isEmpty('张三');    // false

// 检查是否为数字
Validator.isNumber('123');    // true
Validator.isNumber('abc');    // false
Validator.isNumber(123.45);   // true

// 检查数字范围
Validator.inRange(50, 0, 100);    // true
Validator.inRange(150, 0, 100);   // false

// 检查日期
Validator.isValidDate('2025-11-06');  // true
Validator.isValidDate('invalid');     // false
```

#### 文件校验

```javascript
// 检查是否为Excel文件
const file = document.getElementById('fileInput').files[0];
if (!Validator.isExcelFile(file)) {
    showToast('请选择Excel文件', 'error');
    return;
}

// 检查文件大小（默认最大50MB）
if (!Validator.checkFileSize(file, 50)) {
    showToast('文件过大，请选择小于50MB的文件', 'error');
    return;
}
```

#### 批量校验

```javascript
// 定义校验规则
const rules = {
    name: {
        label: '姓名',
        required: true
    },
    age: {
        label: '年龄',
        required: true,
        type: 'number',
        min: 18,
        max: 65
    },
    email: {
        label: '邮箱',
        pattern: /^[^\s@]+@[^\s@]+\.[^\s@]+$/
    }
};

// 执行校验
const data = {
    name: '张三',
    age: 25,
    email: 'zhangsan@example.com'
};

const result = Validator.validate(data, rules);

if (!result.valid) {
    showToast(result.errors.join('\n'), 'error');
    return;
}
```

---

### 5. 日期处理 (DateUtils)

处理各种日期格式。

#### 格式化日期

```javascript
const { DateUtils } = window.AOSmithUtils;

const now = new Date();

// 默认格式：YYYY-MM-DD
DateUtils.format(now);  // '2025-11-06'

// 自定义格式
DateUtils.format(now, 'YYYY年MM月DD日');     // '2025年11月06日'
DateUtils.format(now, 'YYYY-MM-DD HH:mm:ss'); // '2025-11-06 14:30:45'
```

#### 解析日期

```javascript
// 支持多种格式
DateUtils.parse('2025.9.15');    // Date对象
DateUtils.parse('2025/9/15');    // Date对象
DateUtils.parse('2025-09-15');   // Date对象

// 解析失败返回null
DateUtils.parse('invalid');      // null
```

#### 日期范围检查

```javascript
// 检查日期是否在范围内
DateUtils.inRange('2025.9.20', '2025.9.15', '2025.10.8');  // true
DateUtils.inRange('2025.10.15', '2025.9.15', '2025.10.8'); // false
```

---

### 6. Excel处理 (ExcelUtils)

简化Excel文件的读取和导出。

#### 导出Excel

```javascript
const { ExcelUtils } = window.AOSmithUtils;

// 准备数据
const data = [
    { 姓名: '张三', 年龄: 25, 部门: '销售部' },
    { 姓名: '李四', 年龄: 30, 部门: '市场部' },
    { 姓名: '王五', 年龄: 28, 部门: '销售部' }
];

// 导出（文件名会自动添加时间戳）
ExcelUtils.export(data, '员工列表', '员工信息');
// 生成文件: 员工列表_20251106_143045.xlsx
```

#### 读取Excel

```javascript
const fileInput = document.getElementById('fileInput');
fileInput.addEventListener('change', async (e) => {
    const file = e.target.files[0];
    
    try {
        // 读取Excel文件
        const workbook = await ExcelUtils.read(file);
        
        // 获取第一个sheet
        const firstSheetName = workbook.SheetNames[0];
        const worksheet = workbook.Sheets[firstSheetName];
        
        // 转换为JSON
        const data = XLSX.utils.sheet_to_json(worksheet);
        console.log(data);
        
        showToast('文件读取成功', 'success');
    } catch (error) {
        showToast('文件读取失败: ' + error.message, 'error');
    }
});
```

#### 查找表头行

```javascript
// 自动跳过空白行，找到实际表头
const workbook = await ExcelUtils.read(file);
const worksheet = workbook.Sheets[workbook.SheetNames[0]];

// 查找表头行（默认搜索前5行）
const headerRow = ExcelUtils.findHeaderRow(worksheet, 5);
console.log('表头在第', headerRow + 1, '行');

// 从表头行开始读取数据
const data = XLSX.utils.sheet_to_json(worksheet, {
    range: headerRow
});
```

---

### 7. 数字格式化 (NumberUtils)

格式化数字显示。

#### 金额格式化

```javascript
const { NumberUtils } = window.AOSmithUtils;

// 千分位格式化
NumberUtils.formatMoney(1234567.89);     // '1,234,567.89'
NumberUtils.formatMoney(123.4, 2);       // '123.40'
NumberUtils.formatMoney(1000000, 0);     // '1,000,000'
```

#### 百分比格式化

```javascript
// 小数转百分比
NumberUtils.formatPercent(0.8567);       // '85.67%'
NumberUtils.formatPercent(0.8567, 1);    // '85.7%'
NumberUtils.formatPercent(1.2);          // '120.00%'
```

#### 四舍五入

```javascript
NumberUtils.round(3.14159, 2);   // 3.14
NumberUtils.round(3.14159, 4);   // 3.1416
NumberUtils.round(1234.567, 0);  // 1235
```

---

### 8. 调试工具 (Debug)

开发调试辅助工具。

#### 启用调试

```javascript
const { Debug } = window.AOSmithUtils;

// 启用调试模式
Debug.enabled = true;

// 普通日志
Debug.log('开始处理数据');
Debug.log('当前数据:', data);

// 性能计时
Debug.time('数据处理');
// ... 执行操作 ...
Debug.timeEnd('数据处理'); // 输出：数据处理: 123.45ms

// 表格显示
Debug.table([
    { name: '张三', age: 25 },
    { name: '李四', age: 30 }
]);
```

---

## 💡 实际应用示例

### 示例1：文件上传与处理

```html
<input type="file" id="fileInput" accept=".xlsx,.xls">
<button onclick="handleFileUpload()">上传并处理</button>

<script>
async function handleFileUpload() {
    const { ExcelUtils, Validator, showToast } = window.AOSmithUtils;
    
    const fileInput = document.getElementById('fileInput');
    const file = fileInput.files[0];
    
    // 校验文件
    if (!file) {
        showToast('请选择文件', 'warning');
        return;
    }
    
    if (!Validator.isExcelFile(file)) {
        showToast('请选择Excel文件', 'error');
        return;
    }
    
    if (!Validator.checkFileSize(file, 50)) {
        showToast('文件过大', 'error');
        return;
    }
    
    try {
        // 显示进度
        window.progressBar.show('正在读取文件...');
        
        // 读取文件
        const workbook = await ExcelUtils.read(file);
        const worksheet = workbook.Sheets[workbook.SheetNames[0]];
        
        // 查找表头
        const headerRow = ExcelUtils.findHeaderRow(worksheet);
        const data = XLSX.utils.sheet_to_json(worksheet, {
            range: headerRow
        });
        
        window.progressBar.hide();
        showToast(`成功读取 ${data.length} 条数据`, 'success');
        
        // 处理数据...
        
    } catch (error) {
        window.progressBar.hide();
        showToast('文件处理失败: ' + error.message, 'error');
    }
}
</script>
```

### 示例2：保存和恢复配置

```html
<button onclick="saveConfig()">保存配置</button>
<button onclick="loadConfig()">加载配置</button>
<button onclick="resetConfig()">恢复默认</button>

<script>
const { Storage, showToast } = window.AOSmithUtils;

function saveConfig() {
    const config = {
        theme: 'blue',
        showHelp: true,
        autoSave: false
    };
    
    if (Storage.set('appConfig', config, { version: '2.0' })) {
        showToast('配置已保存', 'success');
    }
}

function loadConfig() {
    const config = Storage.get('appConfig', {
        theme: 'default',
        showHelp: false,
        autoSave: false
    });
    
    console.log('加载的配置:', config);
    showToast('配置已加载', 'info');
}

function resetConfig() {
    Storage.remove('appConfig');
    showToast('已恢复默认配置', 'info');
}
</script>
```

### 示例3：数据批量处理

```javascript
async function processLargeDataset(data) {
    const { NumberUtils, DateUtils } = window.AOSmithUtils;
    const batchSize = 500;
    const total = data.length;
    
    window.progressBar.show('正在处理数据...');
    
    const results = [];
    
    for (let i = 0; i < total; i += batchSize) {
        const batch = data.slice(i, i + batchSize);
        
        // 处理批次
        const processed = batch.map(row => ({
            ...row,
            日期: DateUtils.format(row.日期, 'YYYY-MM-DD'),
            金额: NumberUtils.round(row.金额, 2),
            折扣率: NumberUtils.formatPercent(row.折扣率)
        }));
        
        results.push(...processed);
        
        // 更新进度
        const percent = Math.min(100, ((i + batchSize) / total) * 100);
        window.progressBar.update(
            percent,
            `已处理 ${Math.min(i + batchSize, total)}/${total} 条`
        );
        
        // 释放UI线程
        await new Promise(resolve => setTimeout(resolve, 0));
    }
    
    window.progressBar.hide();
    showToast('数据处理完成', 'success');
    
    return results;
}
```

---

## 🎨 样式自定义

### Toast样式

Toast消息使用内联样式，可以通过修改 `showToast` 函数中的样式来自定义外观。

### 进度条样式

可以在创建ProgressBar实例时传入自定义选项：

```javascript
const customProgressBar = new ProgressBar({
    color: '#10B981',  // 绿色进度条
    height: '6px'      // 更粗的进度条
});
```

---

## 🔧 高级用法

### 链式调用

```javascript
// 保存数据并显示消息
if (Storage.set('data', myData)) {
    showToast('保存成功', 'success');
}

// 读取数据并校验
const data = Storage.get('userData');
if (data && Validator.validate(data, rules).valid) {
    processData(data);
}
```

### 错误处理

```javascript
try {
    const workbook = await ExcelUtils.read(file);
    // ... 处理数据
} catch (error) {
    console.error('处理失败:', error);
    showToast('文件处理失败: ' + error.message, 'error', {
        duration: 5000,
        closable: true
    });
}
```

---

## 🌟 最佳实践

### 1. 统一错误处理

```javascript
function handleError(error, context = '') {
    console.error(context, error);
    showToast(
        `${context}失败: ${error.message}`,
        'error',
        { duration: 5000, closable: true }
    );
}

// 使用
try {
    // ... 操作
} catch (error) {
    handleError(error, '数据处理');
}
```

### 2. 配置管理模式

```javascript
class ConfigManager {
    constructor(key, defaultConfig) {
        this.key = key;
        this.defaultConfig = defaultConfig;
    }
    
    get() {
        return Storage.get(this.key, this.defaultConfig);
    }
    
    set(config) {
        return Storage.set(this.key, config, { version: '2.0' });
    }
    
    reset() {
        Storage.remove(this.key);
        return this.defaultConfig;
    }
}

// 使用
const configManager = new ConfigManager('appConfig', {
    theme: 'blue',
    autoSave: false
});
```

### 3. 批处理模式

```javascript
async function batchProcess(data, processor, batchSize = 500) {
    const total = data.length;
    const results = [];
    
    window.progressBar.show('处理中...');
    
    for (let i = 0; i < total; i += batchSize) {
        const batch = data.slice(i, i + batchSize);
        const batchResults = await processor(batch);
        results.push(...batchResults);
        
        window.progressBar.update(
            ((i + batchSize) / total) * 100,
            `${Math.min(i + batchSize, total)}/${total}`
        );
        
        await new Promise(resolve => setTimeout(resolve, 0));
    }
    
    window.progressBar.hide();
    return results;
}
```

---

## 📝 更新日志

### v2.1 (2025-11-06)
- ✨ 初始版本发布
- ✅ Toast消息提示
- ✅ 进度条组件
- ✅ 本地存储管理
- ✅ 数据校验工具
- ✅ 日期处理工具
- ✅ Excel处理工具
- ✅ 数字格式化工具
- ✅ 调试工具

---

## 💬 反馈与支持

如有问题或建议，请联系开发团队。

**版本**: v2.1  
**更新日期**: 2025-11-06

